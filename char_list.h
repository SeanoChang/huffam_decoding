#ifndef CHAR_LIST_H
#define CHAR_LIST_H

long* countChar(char*);

Node* makeSortedLL(long*);

bool addNode(Node*, TreeNode*);

Node* buildNode(TreeNode*);

Node* removeNode(Node*);

void freeNode(Node*);
#endif
