#ifndef CHAR_LIST_H
#define CHAR_LIST_H

long* countChar(char*);

Header* makeSortedLL(long*);

bool addNode(Header*, TreeNode*);

Node* buildNode(TreeNode*);

Node* removeNode(Node*);

void freeNode(Node*);
#endif
